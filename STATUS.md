# STATUS — open items (carried-forward)

Last updated: 2026-07-01 (post-1200). Newest report: reports/2026-07-01-1200.md.
Last checked: 2026-07-01 ~23:00 UTC. **Quiet tick** — no new actionable inbound since the ~22:00 UTC heartbeat. Read-only gh check: no PR state change — sx#2143 still OPEN (BLOCKED/MERGEABLE/REVIEW_REQUIRED, awaiting Wan on catch-narrowing), snapshot.js#1203 still OPEN (MERGEABLE/REVIEW_REQUIRED, unchanged). All carryovers static: (A) snapshot.js#1203 catch-narrowing (await Wan); (B) sequencer isSameAddress swap (await Wan); (C) Discord raid user 1520075759685734462 (await Less ban auth); (D) sx#2143 merge watch; (E) docs.checkpoint.box subdomain confirm from Less; (F) Grafana re-add + /mcp reconnect (await Less). No new report this tick.
Last checked: 2026-07-01 ~22:00 UTC. **Quiet tick** — no new actionable inbound since the ~21:00 UTC heartbeat. Read-only gh check: no PR state change — sx#2143 still OPEN (MERGEABLE/REVIEW_REQUIRED, all checks green incl. lint-build-test + E2E + netlify previews, chai3-bot approved, awaiting Wan on catch-narrowing), snapshot.js#1203 still OPEN (MERGEABLE/REVIEW_REQUIRED, unchanged). All carryovers static: (A) snapshot.js#1203 catch-narrowing (await Wan); (B) sequencer isSameAddress swap (await Wan); (C) Discord raid user 1520075759685734462 (await Less ban auth); (D) sx#2143 merge watch; (E) docs.checkpoint.box subdomain confirm from Less; (F) Grafana re-add + /mcp reconnect (await Less). No new report this tick.
Last checked: 2026-07-01 ~21:00 UTC. **Quiet tick** — no new actionable inbound since the ~20:00 UTC heartbeat. Read-only gh check: no PR state change — sx#2143 still OPEN (BLOCKED/REVIEW_REQUIRED, MERGEABLE, awaiting Wan on catch-narrowing), snapshot.js#1203 still OPEN (MERGEABLE/REVIEW_REQUIRED, unchanged). All carryovers static: (A) snapshot.js#1203 catch-narrowing (await Wan); (B) sequencer isSameAddress swap (await Wan); (C) Discord raid user 1520075759685734462 (await Less ban auth); (D) sx#2143 merge watch; (E) docs.checkpoint.box subdomain confirm from Less; (F) Grafana re-add + /mcp reconnect (await Less). No new report this tick.
Last checked: 2026-07-01 ~20:00 UTC. **Quiet tick** — no new actionable inbound since the ~19:00 UTC heartbeat. Read-only gh check: no PR state change — sx#2143 still OPEN (BLOCKED/REVIEW_REQUIRED, awaiting Wan on catch-narrowing), snapshot.js#1203 still OPEN (BEHIND/REVIEW_REQUIRED, unchanged). All carryovers static: (A) snapshot.js#1203 catch-narrowing (await Wan); (B) sequencer isSameAddress swap (await Wan); (C) Discord raid user 1520075759685734462 (await Less ban auth); (D) sx#2143 merge watch; (E) docs.checkpoint.box subdomain confirm from Less; (F) Grafana re-add + /mcp reconnect (await Less). No new report this tick.
Last checked: 2026-07-01 ~19:00 UTC. **Quiet tick** — no new actionable inbound since ~18:00 UTC. Read-only gh check: no PR state change — sx#2143 still OPEN (BLOCKED/REVIEW_REQUIRED, awaiting Wan on catch-narrowing), snapshot.js#1203 still OPEN (BEHIND/REVIEW_REQUIRED, unchanged). All carryovers static: (A) snapshot.js#1203 catch-narrowing (await Wan); (B) sequencer isSameAddress swap (await Wan); (C) Discord raid user 1520075759685734462 (await Less ban auth); (D) sx#2143 merge watch; (E) docs.checkpoint.box subdomain confirm from Less; (F) Grafana re-add + /mcp reconnect (await Less). No new report this tick.
Last checked: 2026-07-01 ~18:00 UTC. **Quiet tick** — no new actionable inbound to the main Metro session since ~17:00 UTC. Read-only gh check: no PR state change — sx#2143 still OPEN (updated 06-30T17:24Z, all checks green incl. lint-build-test + E2E, chai3-bot approved, awaiting Wan on catch-narrowing), snapshot.js#1203 still OPEN (unchanged since 06-21). All carryovers static: (A) snapshot.js#1203 catch-narrowing (await Wan); (B) sequencer isSameAddress swap (await Wan); (C) Discord raid user 1520075759685734462 (await Less ban auth); (D) sx#2143 merge watch; (E) docs.checkpoint.box subdomain confirm from Less; (F) Grafana re-add + session /mcp reconnect (await Less). No new report this tick.
Last checked: 2026-07-01 ~17:00 UTC. **Quiet tick** — since the ~16:00 UTC heartbeat the only inbound to the main Metro session was wa0x6e in the Pineapple channel asking "and now?" re Grafana MCP; re-checked and Grafana MCP is STILL not connected on this session (only Better Stack + Sentry present), told him it likely needs a `/mcp` reconnect on my session once Less adds it. Carryover (F, was #8): **Grafana re-add STILL pending — and now also needs a session `/mcp` reconnect to pick it up once added.** All other carryovers unchanged: (A) snapshot.js#1203 catch-narrowing (await Wan); (B) sequencer isSameAddress swap of 16 sites (await Wan); (C) Discord raid user 1520075759685734462 (await Less ban auth); (D) sx#2143 merge watch (OPEN, chai3-bot approved); (E) sx#2197 MERGED, docs.checkpoint.box subdomain confirm from Less still open. No new report this tick.
Last checked: 2026-07-01 ~16:00 UTC. **Quiet tick** — no new actionable inbound to the main Metro session since ~15:00 UTC (A-E static). Read-only gh check: no PR state change — sx#2143 still OPEN (updated 06-30T17:24Z, chai3-bot approved, awaiting Wan on catch-narrowing), sx#2197 already MERGED (06-30T21:51Z, review/merge half of E done), snapshot.js#1203 still OPEN (unchanged since 06-21). Carryovers unchanged: (A) snapshot.js#1203 catch-narrowing (await Wan); (B) sequencer isSameAddress swap of 16 sites (await Wan); (C) Discord raid user 1520075759685734462 (await Less ban auth); (D) watch sx#2143 for merge; (E-residual) docs.checkpoint.box subdomain confirm from Less. No new report this tick.
Last checked: 2026-07-01 ~15:00 UTC. **Near-quiet tick — one PR state change.** No new actionable inbound to the main Metro session since ~14:00 UTC (A-D static). Read-only gh check advanced carryover (E): **sx-monorepo#2197 (our checkpoint.box→checkpoint.snapshot.box docs fix) is now MERGED** (2026-06-30T21:51:36Z) — the review/merge half of (E) is DONE; only the docs.checkpoint.box-subdomain confirm from Less remains open under (E). sx#2143 still OPEN (unchanged, updated 06-30T17:24Z, chai3-bot approved, awaiting Wan on the catch-narrowing call), snapshot.js#1203 still OPEN (unchanged since 06-21). No new report file (single small delta folded here). Remaining carryovers: (A) snapshot.js#1203 catch-narrowing (await Wan); (B) sequencer isSameAddress swap (await Wan); (C) Discord raid user 1520075759685734462 (await Less ban auth); (D) watch sx#2143 for merge; (E-residual) docs.checkpoint.box subdomain confirm from Less.
Last checked: 2026-07-01 ~14:00 UTC. **Quiet tick** — since the ~13:00 UTC heartbeat only LURKED non-actionable traffic to the main Metro session: more #api delegates-api/hypersync discussion among Less/Sekhmet (Sekhmet noted hypersync was already on delegates-api), plus a casual "gm" in a general channel — none addressed to me, correctly no action. PART 3 surfaced nothing net-new (KB in maintenance mode, per-MCP map complete). Carryovers all unchanged: (A) snapshot.js#1203 catch-narrowing (await Wan); (B) sequencer isSameAddress swap (await Wan); (C) Discord raid user 1520075759685734462 (await Less ban auth); (D) watch sx#2143 for merge; (E) watch sx#2197 review/merge + docs-subdomain confirm from Less. No new report this tick.
Last checked: 2026-07-01 ~13:00 UTC. **Quiet tick** — main Metro session had only LURKED traffic since ~12:00 UTC (#api human-to-human: Less + Sekhmet + ChaituVR on delegates-api sync duration, co-locating the DB to speed syncing, and a future feature idea to track indexing start / in-sync time — none addressed to me, correctly no action). Carryovers all unchanged: (A) snapshot.js#1203 catch-narrowing (await Wan); (B) sequencer isSameAddress swap (await Wan); (C) Discord raid user 1520075759685734462 (await Less ban auth); (D) watch sx#2143 for merge; (E) watch sx#2197 review/merge + docs-subdomain confirm from Less. No new report this tick. (Noted, not actioned: the "track indexing start + in-sync time" feature idea is a candidate for a future potential-tasks note if the team picks it up.)
Last checked: 2026-07-01 ~12:00 UTC. **Substance tick** — NEW: opened DRAFT PR snapshot-labs/sx-monorepo#2197 (branch fix/checkpoint-box-url off master) replacing `https://checkpoint.box` → `https://checkpoint.snapshot.box`, docs/README only (4 files: README.md, docs/snapshot-x/services/api.mdx, apps/api/README.md, apps/delegates-api/README.md), no code, after-grep clean, NO resync (confirmed to Less in #api). Open Q to Less: whether the `docs.checkpoint.box` subdomain change is also wanted. See reports/2026-07-01-1200.md. Carryovers unchanged: (A) snapshot.js#1203 catch-narrowing (await Wan); (B) sequencer isSameAddress swap of 16 sites (await Wan); (C) Discord raid user 1520075759685734462 (await Less ban auth); (D) watch sx#2143 for merge; plus (E) watch #2197 for review/merge + the docs-subdomain confirm.
Last checked: 2026-07-01 ~11:00 UTC. **Quiet tick** — no new inbound to the main Metro session since ~10:00 UTC. Carryovers all still open/unchanged: (A) snapshot.js#1203 catch-narrowing push (await Wan); (B) sequencer isSameAddress swap of 16 sites (await Wan); (C) Discord raid user 1520075759685734462 (await Less's ban auth, no destructive action taken); (D) watch sx#2143 for merge. No new report this tick.
Last checked: 2026-07-01 ~10:00 UTC. **Quiet tick** — inbox handled by main Metro session (ChaituVR Q&A on sx PR #2123 = his own MERGED "feat(ui): link proposal Start/End to block explorer" stacked on #2044, pure Q&A no action; lurked Sekhmet→Less delegates-api DNS request — delete CNAMEs for delegates-api-1/-2, set A to 129.212.146.236 — human-to-human, correctly no action). No new actionable items. Carryovers unchanged: snapshot.js#1203 catch-narrowing push (await Wan); sequencer isSameAddress swap of 16 sites (await Wan); Discord raid user 1520075759685734462 (flagged to Less, await ban auth, no destructive action); watch sx#2143 for merge. No new report this tick.
Last checked: 2026-07-01 ~09:00 UTC. **HIGH-activity tick (delta 0900):** (a) **sx PR #2169** (ours, sequencer addr normalization) **CLOSED** — Wan right, ingestion already enforces EIP-55 checksum via snapshot.js `validateSchema format:"address"`; lowercasing would regress dedup/leaderboard/follows. (b) **sx PR #2143** (resolver-agnostic ENS, ours) — catch narrowed to `CALL_EXCEPTION` + re-throw infra, commit `b5686661`, chai3-bot **approved**. (c) **Sequencer ENS fix already = ChaituVR's snapshot.js#1203** (green); only Wan's fallback-catch narrowing outstanding — **awaiting Wan's call** on pushing to Chaitu's branch. (d) **Address-util:** no shared equality helper; recommended local `isSameAddress` to replace 16 ad-hoc `toLowerCase` sites across 6 sequencer writer files — **awaiting Wan's go**. (e) **ACTIVE Discord raid** — user `1520075759685734462` mass-posted one image across 10+ channels; flagged to Less, recommended ban+bulk-delete, **awaiting auth** (no destructive action). Couldn't read image bytes (on metro host). Better Stack OK; X read-only; Grafana re-add pending. See reports/2026-07-01-0900.md.
Last checked: 2026-06-30 ~17:08 UTC. **Quiet tick** — inbox handled by main Metro session (long lurked human-to-human NickAI discussion: Less + Sekhmet on PRs #358/#362/#365, BetterAuth MCP-plugin deprecation vs generic oauth-provider, search_polymarket tool — none addressed to me/Metro, correctly no reply). No new actionable items. Carried-forward unchanged: origin-push of MCP fix commits (#1, pending reconcile), Grafana MCP re-add + pineapple k8s-event confirmation (pending Less/cluster access), X-posting read-only, Better Stack working. No new report this tick.
Last checked: 2026-06-30 ~16:08 UTC. **Tick 1608 delta:** (a) **Better Stack MCP RECONNECTED** + working (read calls back). (b) **X MCP confirmed app-level only** — `x-cloud` is read/bookmark; posting needs user-context OAuth, still UNAVAILABLE (re-confirmed, no change). (c) **Pineapple outage RCA DONE** — 2026-06-28 22:31 UTC pod `pineapple-54d65f6684-6ljs4` restart, ~11min log silence; no deploy / no master merge / no Sentry error → **infra-level pod kill (OOM/eviction/node drain)**; k8s events for the pod still UNCONFIRMED (no cluster access from loop session). (d) **Grafana MCP re-add REQUESTED** — wa0x6e asked Less to re-add Grafana to tony's MCP (not yet present; would let future pineapple-style RCAs confirm pod-kill cause directly). Inbox this hour handled by main Metro session. See reports/2026-06-30-1608.md.
Last checked: 2026-06-30 ~15:00 UTC. **Tick 1500 delta:** (a) **X MCP status CORRECTED** — the dead `x` :8000 entry is REPLACED by connected cloud `x-cloud` → `https://api.x.com/mcp` (xmcp, bearer-auth, ✔ Connected, 24 tools) but it is **READ/BOOKMARK-ONLY (no create_post)** → X-posting STILL unavailable, now for a token-scope reason; tools not loaded in current session (needs reconnect). KB updated (socials-and-external.md, mcp/README.md, mcp/oauth-gated-and-misc.md). (b) **Sekhmet #interns open-PR origin-trace = DONE** — 7/9 PRs referenced (Discord #2195/#2185/#2086/#2122; off-Discord-documented #2103/#2168/#2143), 2 gaps (#2169/#2119) are the telegram-user tooling limit. (c) Open/awaiting-human (Last checked ~2026-06-30 15:00 UTC): Chaitu on schnaps-api #21 design; Less on X-posting enablement path (just clarified x-cloud is read-only), Stripe wiring, #2185 👍; the held #general community-support question (knoshua, premium voting strategies — bot awaiting Less's nod on whether to field #general); origin-push of MCP fix commits (#1, pending). **(Prior tick post-1315** — three Metro asks served + two new capability gaps surfaced (see reports/2026-06-30-1315.md): (a) **Sekhmet (#interns) open-PR origin-trace DELIVERED** — 9 open sx-monorepo PRs (author tony8713) traced to their Discord asks: 4 traced (#2195←Sekhmet, #2185←Less, #2168←BetterStack/self, #2143←Intercom), 5 untraced (#2169 self-hardening + #2122/#2119/#2103/#2086 predate the searchable Discord window); posted as INLINE replies under her anchor; offered to deep-page the 5 untraced — **awaiting Sekhmet.** (b) **NEW GAP: no Discord thread support in the loop session** — no `discord` tool registered (only `mcp__metro__*` + `claude_ai_*`); metro has no thread verb, so "reply in a thread" can only be served as an inline reply (`reply_to`). Added as hardening-plan #8 (P2); documented in `knowledge-base/mcp/metro.md`. (c) **Less asked the bot to "tweet" → X-posting UNAVAILABLE** — `x` MCP down (no :8000 listener) AND 0 Zapier X actions enabled; bot offered 3 enablement paths (bring up x MCP / Zapier OAuth / Typefully) — **awaiting Less's choice**; documented in `knowledge-base/socials-and-external.md`. Prior watch-items (human decisions, bot stays out unless pinged): (1) **schnaps-api #21** — bot was asked by Chaitu to resolve merge conflicts; turned out to be a competing-architecture design call (#21 `refactor/stripe-base-indexer` = BaseIndexer + injected-writers + extracted-provider + subscription-cancellation vs base `feat/stripe-subscription-payment` = inline-provider + window-indexing + partial-refunds + cursor). Bot kicked it back to Chaitu for the canonical-design decision. Team (Less/Sekhmet/Chaitu) is settling it in #api — emerging consensus: consolidate to ONE PR, keep #21's BaseIndexer approach, "merge 21 into 20 first." **WATCH #api for the final word, then proceed on #21 + relay to Chaitu.** (Note: Less told the bot "Not asked you anything" in #api — stay out of that thread unless pinged.) (2) **Stripe** — Less posted "Stripe account is ready" (live mode activated, screenshot) in the gtm channel; bot asked whether to wire the live key/webhook into schnaps-api now or after #21 — **awaiting Less. Stripe wiring is gated on the #21 consolidation.** (3) **#2185** still awaiting Less's 👍 on the cherry-pick-UI-only plan. MCP connection stable post per-session-transport fix. **MCP stream-wedge ROOT-CAUSED + FIXED + DEPLOYED** — the read-verb 300s timeouts / zombie-stream / post-deploy flakiness are GONE at the source (per-session transports replace the single global `rebind()`'d transport; commit `04f6b6f`, build clean + 137 tests, live on Fly app `metro`, reads return instantly). **CAVEAT:** fix commits `04f6b6f` + `2f9a5fa` are on **LOCAL main only — NOT pushed to origin** (branch 5 behind origin via unrelated telegram-user commits); cloud runs the fix (deploys from working tree) but **origin needs reconcile + push**. **Local `box.metro.dispatcher` RETIRED** (`launchctl bootout`, plist kept) → one client per inbox, kills the multi-client rebind thrash. **telegram-user inbound = NOT IMPLEMENTED** (no MTProto/user train, bot tokens only) — build follow-up, not a regression. **#2125 `active_proposal_count` VERIFIED ON REAL LIVE-INDEXED DATA (2026-06-30 ~09:30 UTC) — not synthetic.** Indexed sepolia snapshot-x space `0x91f5592139D82Af57Ea4E1D73659690F89f965c0` (2 proposals, max_end 2033 = permanently active) on `feat/active-proposals-explore`@`47b267b4` + checkpoint beta.73 (sep start block 7107600 so SpaceCreated+proposals replay; reached in ~25s); live query → `active_proposal_count=2`, filter boundary correct (`_gt:1`→hit, `_gt:2`→[]). Cross-checked: SQL with the resolver's exact predicate = 2, and the production testnet API independently reports the same 2 active. Sekhmet's "you didn't actually test it / why test with an empty source set" pushback ADDRESSED and reported in #api (`1521447976592216214`). Mainnet not feasible in-window (only active onchain props are a Feb-2024 Starknet space needing ~10.7M-block replay + an OZ Governor space). **Now awaits #2125 review/merge → retarget UI PR #2195 to master.** Comms caught up post-fix (Sekhmet pushback conceded; Less interns/avatar pings acked). See reports/2026-06-30-0900.md + `knowledge-base/hardening-plan.md`.
Prior substantive tick (2026-06-29 ~18:30 UTC, see report): **#2125 split EXECUTED** (now backend-only, force-push 9f51f96e; UI → new stacked PR #2195 on feat/active-proposals-explore — retarget to master after #2125 merges); **checkpoint#390 confirmed MERGED + released v0.1.0-beta.73**; `active_proposal_count` `@computed` **VERIFIED live** (2 branch fixes flagged to Sekhmet: bump apps/api checkpoint dep ≥ beta.73; rebase on master for `quorum_finalized` from #2172); **XMTP send outage ROOT-CAUSED** — cloud `tony` client never boots (raw-privateKey loader patch MISSING from deployed Fly image; local-only uncommitted edit; per-IP bucket R152…DEF on Fly shared egress IP, restart won't clear) → fix "Deploy + DERIVE_COUNT=1" approved, **deploy in progress** (reads OK); Chaitu↔Notion confirmed (Avalanche-premium-networks Notion lookup in progress); #2185 rebase still BLOCKED awaiting Less; "x" MCP still unreachable.

> **SX vote saga (summary):** diagnosis **COMPLETE** — app layer is clean, loss is a silent 0-row / non-durable write at the psdb DB-gateway. **PR #2189 CLOSED** (Wan). Wan's two logs (06-27) were both the wrong type — Postgres **statement logging isn't capturing INSERTs** (`log_statement` ≠ `mod`/`all`), so the two past missing votes (15:11:13Z & 19:48:55Z) are **unknowable from logs**. **AWAITING Wan's decision** on flipping `log_statement='mod'`/`log_min_duration_statement=0` (forward fix, no app change) — or PlanetScale Query Insights. **#2125 split awaiting go.**

## Open / next
- **XMTP send-path RECOVERED (2026-06-29 ~19:30 UTC).** The cloud Fly deploy shipped the raw-privateKey loader patch + `DERIVE_COUNT=1`; the `tony` client booted cleanly (synced 15 conversations, no rate-limit), and a real send to Less went through successfully — closing the prior PART 4 next-focus. Loader patch is now committed/deployed (no longer deploy-drift). *(History: per-IP rate limit on `GetIdentityUpdatesV2` "resource exhausted" node `R152.233.42.56DEF`; reads always worked; root cause was the deployed Fly image missing the raw-privateKey loader patch so the cloud `tony` client never booted; fix was "Deploy + `DERIVE_COUNT=1`".)* Mitigations from `skills/metro-comms-reliability.md` stay in place; re-arm a short-interval poll only if an XMTP thread goes hot. Failure-mode write-up: `skills/xmtp-multi-client-installation-cap.md`.
- **X MCP CORRECTED → `x-cloud` CONNECTED but READ/BOOKMARK-ONLY (2026-06-30, supersedes the stale ":8000 down" note).** The dead `x` :8000 entry is REPLACED by `x-cloud` → `https://api.x.com/mcp` (X's official hosted MCP "xmcp", bearer-auth). `claude mcp list` ✔ Connected, 24 tools (search posts/news/users, timelines/mentions/trends, get_users_me, create/delete *bookmarks*) — but **NO create_post/tweet tool** → **X-POSTING STILL UNAVAILABLE** (token tier = read+bookmark, no publish). `x-cloud` tools not loaded in current session (added post-start → needs reconnect to surface reads). To enable posting (awaiting Less): write-capable X token tier, Zapier X write OAuth, or Typefully (15/mo cap). See `knowledge-base/socials-and-external.md` + `mcp/oauth-gated-and-misc.md`.
- **Sekhmet (#interns) open-PR origin-trace — DONE (2026-06-30).** Bot delivered Discord/other origins for all 9 open sx-monorepo PRs under her #interns anchor: Discord origins #2195/#2185/#2086/#2122 (jump links); off-Discord-but-documented #2103 (Fabien via Telegram, recorded in #api), #2168 (BetterStack logs), #2143 (Intercom); #2169/#2119 have no reachable Discord origin (likely Telegram — not searchable until telegram-user inbound is built). **7/9 referenced; the 2 gaps are the telegram-user tooling limit, not a miss.**
- **sx-monorepo #2185 rebase — BLOCKED, awaiting Less's decision (2026-06-29).** Branch's backend commits are superseded by #2181 on master; plan is to cherry-pick ONLY the UI commit `66db2dfd` once Less confirms. (Was a missed/dropped Discord request, now picked up.)
- **checkpoint #390 — MERGED + released `v0.1.0-beta.73` (confirmed 2026-06-29).** Sekhmet's review comments were all in master pre-merge; nothing left to fix. Reported to Less. (Was "addressing review comments".)
- **Chaitu — Avalanche info for premium networks in Notion (2026-06-29).** Chaitu confirmed connected-to-Notion, then requested Avalanche details added to the premium-networks Notion page. A worker is doing the lookup now (**in progress**).
- **boorger.eth expiry — TRIAGED, no action (2026-06-29).** Personal Ledger EOA `0x1210…a3DC`, in premium-decay auction; NOT a Snapshot space admin/member/mod, no proposals, zero refs in Stage/bonustrack — personal to Boorger only.
- **stamp #468 (snapshot-labs/stamp) — DROPPED (retracted by Wan).** "Add zod validation — Part 1: JSON-RPC boundary" (split from PR #451). Wan **retracted** it ("forget it, you're dropping too much message to be useful") — earlier dropped events made Tony unreliable for that real-time task. The other agent (`1471598965756788970`) **retains** it. Tony acknowledged gracefully; do **NOT** pick it up unless explicitly reassigned.
- **sx-monorepo #2125 — rebase + checkpoint-bump + re-verify DONE, force-pushed `47b267b4` (2026-06-29 ~21:05 UTC, Sekhmet's request).** `feat/active-proposals-explore` **rebased clean onto master** (now carries `quorum_finalized` from #2172 → the proposal-insert blocker is RESOLVED); `apps/api` checkpoint dep **bumped to `^0.1.0-beta.73`** (was beta.70); build + lint pass; `active_proposal_count` `@computed` **re-verified working**. **LIVE-DATA RE-VERIFICATION DONE (2026-06-30 ~09:30 UTC):** Sekhmet rejected the earlier synthetic-row proof; re-did it on REAL indexed data — sepolia space `0x91f5592139D82Af57Ea4E1D73659690F89f965c0` (2 active props), `active_proposal_count=2`, filter boundary correct, SQL + production testnet API cross-checks both = 2. Reported to Sekhmet in #api (`1521447976592216214`). **Now awaits review/merge → then retarget UI PR #2195 to master.** (Pre-existing, non-blocking: checkpoint local-indexing quirk — `getConfigStartBlock` `Math.min` over empty sources → `Infinity` — affects master/beta.72/beta.73 alike, does NOT block #2125.) *(History: #2125 split EXECUTED 2026-06-29 — made backend-only, force-push `9f51f96e`, 4 UI files reverted to merge-base; UI half → stacked PR #2195 on `feat/active-proposals-explore`.)*
- **checkpoint#390 (`@computed`) — utACK/COMMENT (sekhmet-review).** Wiktor is author+reviewer. Open blocker: `getNestedResolver` passes plural `columnName` into `getLoader` (needs singular entity name). New finding: computed-where loop ignores `prefix` in nested recursion (`resolvers.ts:149`) → nested where-key hijack risk. Relayed to Less, not posted to GitHub. #2125 depends on this.
- **SX API k8s migration (Wiktor, 2026-06-27).** arb1 synced in 24.5h on k8s ($48 server, no manual indexes); Base failed (edge RPC block-range limits, fixed in new Checkpoint). Plan: release new Checkpoint + drop old droplets → fully k8s-hosted API.
- **Loop is now SELF-DIRECTED** — `LOOP.md` (commit 92546d0) is the editable loop prompt; the cron job just pulls the repo + reads & runs LOOP.md (so the loop can self-improve). Repo reframed as "Tony memory" (Tony = assistant/identity; Metro = the comms daemon/bot account). `knowledge-base/` seeded (mcp-servers, accounts-and-stations, repos-and-infra, socials-and-external, README).
- **#2186** (sx-monorepo) — **DB-CONFIRMED lost write, app-side pre-commit.** Read-only DB dig (PG 17.10, Patroni HA) found id-sequence GAPS (7 missing ids in 1–40; id 39 between id 38 and 40) = `nextval` consumed but row never committed. RULED OUT pooler/idle-timeout (all timeouts 0, direct :5432, no PgBouncer) and server-side rollback/deadlock (xact_rollback=1 lifetime, 0 deadlocks) → loss is APP-SIDE before COMMIT. Best-fit: old `.onConflict().ignore()` no-op on a (sender,hash) hit, or an abandoned knex txn. **PR #2189 CLOSED** (was the detection layer). **Real fix pending: relayer self-heal (option 2 — watch `CommitAdded`/scan `_commits` and self-register, recovers orphaned commits) OR client retry on the rpcError.** Exact per-statement error needs **PlanetScale Query Insights (dashboard)** — `pg_stat_statements`/`pgaudit` not installed, not reachable via SQL. **2026-06-27 conclusive mana-log dig on the two missing votes: NOTHING anomalous** (register logged, pid 19 throughout/no restart, no "Failed to register transaction", no connection/pool/timeout, loop uninterrupted — yet no row; only unrelated errors: 3095× "Failed to process proposal" RpcError/herodotus from 19:58, "timestamp in the future", "Failed to get data by message hash"). Confirms loss is below the app layer.
- **mana deferred follow-ups** — fix ladder: (1) client/relayer retry on the rpcError (now idempotent — safe), (2) relayer self-heal (recommended), (3) knex pool `afterCreate` ping / txn-wrap the insert so a dropped psdb connection rejects instead of silently no-op'ing.
- **#2183** (`fix/ledger-vote-via-tx`) — **VERIFIED HEALTHY end-to-end** (voter 0x59a1748B…54Eb4, space 0x009fedaf…682a4 prop 15, mana row id 40, indexed ~4 min). Affected voter can vote on the PR deploy. Independent of #2186.
- **#2189** (sx-monorepo) — **CLOSED (Wan, 2026-06-27); branch `fix/mana-register-transaction-verify-write` kept.** Rationale: master ALREADY has a handler-level try/catch (thrown errors already caught+500'd) → an added try/catch is REDUNDANT; the real lost write is a **silent 0-row / non-durable write with no throw** that no try/catch can catch; and the existing "Registering transaction" log already has sender+hash for diagnosis. Only Query Insights / rowCount-inspection / dropping `.ignore()` surfaces the no-op. **Logs exhausted → PlanetScale Query Insights for 15:11:13Z & 19:48:55Z is the source of truth (asked Wan to pull it).** See skills/diagnose-sx-vote-pipeline.md.
- **SX Ledger thread 15-min poll cron (job `ac435a5e`) — RETIRED (2026-06-27).** The daemon recovered (push + sends healthy, gap-free sweeps) and the SX Ledger thread has been dormant ~11h (vote saga parked, awaiting Wan's `log_statement` decision), so the dedicated poll was steady-state overhead. The hourly PART 1 inbox sweep covers `metro://discord/d0/1517956186929102869`. **Re-arm a short-interval poll only if drops recur OR a thread goes hot again** (see LOOP.md PART 1).
- **Metro daemon reliability — RECOVERED (2026-06-27, post-1408).** After a **/mcp reconnect**, Less's count-sweeps (1–10) across ALL stations (Discord, telegram-user, telegram t0, XMTP) arrived **COMPLETE with no gaps**; sends no longer timing out. The degraded-both-directions condition (inbound gaps + 300s outbound timeouts) appears **CLEARED**. Cost a task first (stamp #468 retracted). **Keep the read-back / inbox-sweep / hot-thread-poll mitigations in place** — push is still treated as lossy by default; **escalate a daemon restart/reconnect sooner** next time degradation is detected. See skills/metro-comms-reliability.md.
- **#2185** — UI split off #2181; merge after the #2181 backend lands.
- **Orphaned EthTx commits** — valid/permanent on L1 but never indexed on L2; no cheap re-trigger path (needs original metadataUri/reason, not recoverable from chain).
- **#636** (bonustrack/stage, ChatKit) — CI gate red (build passes; lint/typecheck/knip/madge/test fail). Testable via Expo preview + `stage://` deep links; not merge-ready.
- **gtm / Typefully — low-priority context (not a task).** Amalio's content flow: JARVIS approval → Typefully (intermediate) → analytics. Analytics is a Typefully pro feature (X API usable). Typefully caps 15 posts/month so API push isn't viable — he'll post from the dashboard. See contacts/amalio.md.
- **Trusted-5 identity mapping — COMPLETE 2026-06-26.** All 5 trusted Snapshot Labs members are now mapped: Less = `bonustrack_` (■); Wan = `wa0x6e` (▶); Chaitu = `chaituvr` (▶); Wiktor = Sekhmet/`0cf5e` (▶, contacts merged into `contacts/wiktor.md`, `contacts/sekhmet.md` removed); Amalio = `amaliohidalgo`/1285995520174592085 (▶, confirmed via Less @-mention in the gtm channel).

## Healthy / resolved recently
- All 3 stations live (telegram-user restored after the Jun 25 cutover regression).
- Stage transactions UI rendering cleanly (Less's 20k USDC History screenshot).
- Safe-signed Snapshot vote (gnosis.eth GIP-151, Safe 0xCa1c…BFFb) pushed to the hub and verified live; fully done, nothing carried forward.
