# PROGRESS.md — knowledge-base self-improvement tracker

The single source of truth for what's mapped, what's stale, and what to do next. Each hourly tick reads this first (PART 3 of LOOP.md), advances one meaningful step, then updates this file. Keep it honest and high-signal.

Legend: **Mapped** = done & verified · **Partial** = started, needs deepening/verification · **TODO** = not started.

## Mapped
- `knowledge-base/mcp-servers.md` — now a thin **index/pointer** into `mcp/` (server → file map + deferred-tools caveat); full detail lives in the per-server files.
- `knowledge-base/accounts-and-stations.md` — comms identities (XMTP/Discord/Telegram) + stations + per-station capabilities (public IDs only).
- `knowledge-base/repos-and-infra.md` — repos, local paths, relayer/monitoring/deploy infra.
- `knowledge-base/socials-and-external.md` — reach + X/Typefully/JARVIS.
- Top-level structure: `README.md`, `INDEX.md`, `STATUS.md`, `reports/`, `contacts/`, `skills/`, `preferences/`, `memory/`.
- `knowledge-base/mcp/` — per-MCP tool-by-tool files: `metro.md`, `snapshot.md`, `better-stack.md`, `zapier.md`, `sentry.md`, `tenderly.md`, `notion.md`, `intercom.md`, `netlify.md`, `fireflies.md`, `snapshot-mysql.md` **Done** (real read-only calls captured); `oauth-gated-and-misc.md` (Cloudflare/Calendly/Docusign/Slash/Browserbase) **Done (schema-only)**; `google-workspace.md` **Done (2026-06-28, structure-only)**. Index in `mcp/README.md`. Per-MCP map complete.
- `knowledge-base/built-in-tools.md` **Done (2026-06-27)** — non-MCP harness tool catalog (Agent + sub-agent types, Bash/Read/Write/Edit, ToolSearch, Skill, Monitor/WebSearch/WebFetch, SendMessage/TaskStop/Worktree) + the available Skill list.
- `mcp/snapshot-mysql.md` (2026-06-27) — Snapshot_MySQL REACHABLE: read-only `snapshot-hub@replica`, 12 tables; proposals (34c) + votes (15c, PK voter+space+proposal) schema captured (names only, NO rows). Distinct from the mana/PlanetScale relayer DB.
- `mcp/oauth-gated-and-misc.md` (2026-06-27) — **Slash** REACHABLE (banking/spend API, 100+ endpoints, financial PII → only `GET /tokens/prices` safe-public, not exercised); **Cloudflare/Calendly/Docusign** OAuth-gated (auth NOT started, verify in main); **Browserbase** tools listed but no session opened.
- `knowledge-base/channels/` Discord rosters — **ALL known Discord lines verified 2026-06-27** via metro `read`: SX Ledger thread `1517956186929102869`, #api `1030772407226601522`, interns `1478881436168880218`, plus the last two `1125390177888645231` (team/payment-feed: Less+Amalio+Metro, snapshot/Schnaps bots) and `1508453453930692669` (1:1 DM tonyshot26↔Metro, setup/onboarding). No `(unverified)` Discord rows remain.
- **Per-MCP capability map — COMPLETE (2026-06-27)**, now ongoing-maintenance. All reachable servers mapped tool-by-tool in `mcp/`; only `google-workspace.md` remains a deliberate (privacy) stub.
- **STRUCTURE/REFACTOR pass — DONE (2026-06-27, post-1010).** `INDEX.md` now links EVERYTHING (README/LOOP/STATUS/PROGRESS, all top-level KB files, `mcp/` + every per-server file, `channels/`, `built-in-tools.md`, all skills/preferences/memory files, contacts/README + each contact, reports/). `mcp-servers.md` demoted to a thin **index/pointer** into `mcp/` (de-duplicated; no info lost). `knowledge-base/README.md` Files table extended (mcp/, built-in-tools.md, channels/). Contacts ↔ channels cross-linked **both ways** (each trusted-5 contact got a "Key channels" block; `channels/README.md` roster already points to contacts/ files).

> Key per-server findings (Notion live teamspace `General` + Fabien/bots; Intercom workspace `n9dmxtcs`/59 articles; Netlify 3 teams incl. stagelabs→stage.box + snapshotlabs→11 SX sites; Sentry org `snapshot-labs`/16 projects/NO mana; Tenderly default project + ~100 chains; Zapier none-enabled; Snapshot_MySQL `snapshot-hub@replica`/12 tables) live in the respective `mcp/*.md` files — no longer duplicated here.

## Partial / open
- **`knowledge-base/channels/`** — index + **all known Discord lines verified (2026-06-27)**; cross-linked to contacts/. No unverified Discord rows remain. Optional next: split high-traffic channels into per-channel files.
- **`knowledge-base/mcp/google-workspace.md`** — **DE-STUBBED 2026-06-28** (structure-only). Drive left schema-only by choice (file names sensitive). Nothing further needed.

## TODO
- Nothing net-new outstanding. (All Discord lines now verified.) Optionally de-stub Google Workspace, then operate in maintenance mode (re-verify drifted facts; keep STATUS/reports fresh).

## Next focus (for the next tick)
This tick (2026-06-29, post-1830) **completed** the configured-vs-live MCP reconciliation: audited `~/.claude.json`, found the only configured-but-unreachable static-HTTP endpoints are **`x`** (top-level) and **`unreal-mcp`** (project `/`) — **both** point at the same dead local `127.0.0.1:8000/mcp` (no listener). Project-scoped `chrome-devtools-mcp` and `metro-http` are inactive in the current project scope. All rich servers come from the **claude.ai connector layer** (incl. new **NickAI**, OAuth-style, not exercised), NOT local config. Reconciliation table written into `mcp/README.md`. Also added skill `skills/xmtp-multi-client-installation-cap.md` (XMTP send-outage failure mode + fix sequence: dedup local client → revoke stale installations → ensure raw-privateKey loader patch is DEPLOYED + set `DERIVE_COUNT=1`).

**XMTP send-path recovery — DONE (2026-06-29 ~19:30 UTC).** The Fly deploy shipped the raw-privateKey loader patch + `DERIVE_COUNT=1`; the cloud `tony` client booted cleanly (synced 15 conversations, no rate-limit) and a real send to Less succeeded end-to-end. STATUS XMTP item flipped to RECOVERED; loader patch now committed/deployed (no deploy-drift). Closes the prior PART 4 next-focus.

**MCP stream-wedge FIXED + DEPLOYED (2026-06-30 ~09:00 UTC).** Root-caused the read-verb 300s timeouts / zombie-stream to the single global `rebind()`'d transport in `apps/mcp/src/mcp/index.ts`; replaced with per-session transports (commit `04f6b6f`), build clean + 137 tests, live on Fly app `metro`. Retired the competing local `box.metro.dispatcher` (closes the prior "optional cleanup"). Documented in `knowledge-base/mcp/metro.md` + `knowledge-base/hardening-plan.md`. See reports/2026-06-30-0900.md.

**Concrete next focus (for the next tick), in priority order:**
1. **Push the fix commits to origin** — reconcile the unrelated telegram-user divergence (branch 5 behind origin), then push `04f6b6f` + `2f9a5fa` to main. Cloud already runs the fix (deploys from working tree) but origin is stale.
2. **Hardening P1s** — (a) inbound liveness monitoring/alerting (detect a wedged/zombie inbound stream proactively); (b) reconnect backfill/resumability (needs an `EventStore` so a reconnect replays missed events). Both from `knowledge-base/hardening-plan.md`.
2b. **schnaps-api #21 consolidation (WATCH → ACT once humans decide).** Bot was asked by Chaitu to resolve #21 conflicts; it's actually a competing-architecture design call (#21 `refactor/stripe-base-indexer` = BaseIndexer + injected-writers + extracted-provider + subscription-cancellation vs base `feat/stripe-subscription-payment` = inline-provider + window-indexing + partial-refunds + cursor). Kicked back to Chaitu. Team settling in #api — emerging consensus: consolidate to ONE PR, keep #21's BaseIndexer approach, "merge 21 into 20 first." Watch #api for the final word, then execute the consolidation on #21 + relay to Chaitu. (Less said "Not asked you anything" in #api — stay out unless pinged.)
2c. **Stripe live-key wiring (GATED on #21).** Less posted "Stripe account is ready" (live mode, screenshot) in the gtm channel; bot asked whether to wire live key/webhook into schnaps-api now or after #21 — awaiting Less. Do NOT wire until the #21 consolidation lands and Less confirms.
3. **Wire the `discord` thread tool (P2, new 2026-06-30).** metro has **no thread verb** and the `discord` tool (skill docs `threadCreate`/`threadReply`) is **not registered in the loop session**, so "reply in a thread" requests can only be served as **inline replies** (`reply_to` quoting the anchor). Surfaced when Sekhmet's #interns PR-trace request was answered inline. Wire the `discord` tool or add a metro `thread_create`/`thread_reply` verb. Tracked as hardening-plan #8. Documented in `knowledge-base/mcp/metro.md`.
4. **X-posting enablement (updated 2026-06-30 1500, await Less's chosen path).** X MCP status CORRECTED: the dead `x` :8000 entry is replaced by **`x-cloud` → `https://api.x.com/mcp`** (xmcp, bearer-auth, ✔ Connected, 24 tools) — but it is **READ/BOOKMARK-ONLY (no create_post)** so **X-posting is STILL unavailable** (token tier = read+bookmark, no publish). `x-cloud` reads aren't loaded in the current session (needs reconnect). To enable posting (await Less): (a) write-capable X token tier (exposes create-post), (b) Zapier X write OAuth, (c) Typefully (15-post/mo cap). Do NOT promise a tweet went out until one is enabled. Documented in `knowledge-base/socials-and-external.md` + `mcp/oauth-gated-and-misc.md` + `mcp/README.md`.
5. **#2125 `active_proposal_count` live-data re-verification — DONE (2026-06-30 ~09:30 UTC).** Re-verified on REAL indexed data (not synthetic): sepolia space `0x91f5592139D82Af57Ea4E1D73659690F89f965c0` (2 active props), `active_proposal_count=2`, filter boundary correct, SQL + production testnet API cross-checks both = 2. Sekhmet's "you didn't actually test it" pushback addressed + reported in #api (`1521447976592216214`). **Remaining:** await #2125 review/merge → retarget UI PR #2195 to master. And **Less's #2185 rebase decision** (cherry-pick UI commit `66db2dfd` only) — execute on confirm.
6. **Sekhmet's open-PR origin-trace — DONE (2026-06-30 1500).** Bot delivered references for all 9 open sx-monorepo PRs under her #interns anchor: Discord origins #2195/#2185/#2086/#2122 (jump links); off-Discord-but-documented #2103 (Fabien via Telegram, recorded in #api), #2168 (BetterStack logs), #2143 (Intercom); #2169/#2119 have no reachable Discord origin (likely Telegram — not searchable until telegram-user inbound is built). **7/9 referenced; the 2 gaps are the telegram-user tooling limit, not a miss.**

7. **#general community-support scope (await Less's nod).** A held #general question (knoshua, premium voting strategies) is awaiting Less's go on whether the bot should field community-support questions in #general at all. Do NOT answer in #general until Less confirms the scope.

8. **Grafana MCP re-add (pending Less; re-checked 2026-07-01 ~17:00 — STILL not connected).** wa0x6e asked Less to re-add Grafana to tony's MCP, and pinged again ("and now?") on 2026-07-01. Re-verified this session: still NOT in the connector set (only Better Stack + Sentry present). **Two-step unblock:** (1) Less adds Grafana to the connector set, THEN (2) run a `/mcp` reconnect on my session to actually surface the tools (added-post-start servers don't load until reconnect). Once live, verify read-only access and use it to confirm pineapple-style pod-kill causes (k8s events / node metrics) directly — closes the "k8s events unconfirmed" gap. Track as capability-pending.
9. **Pineapple k8s-event confirmation (NEW 2026-06-30 1608, low urgency).** The 2026-06-28 22:31 UTC pineapple pod `pineapple-54d65f6684-6ljs4` restart was RCA'd to an infra-level pod kill (no deploy/merge/Sentry), but the exact cause (OOMKilled vs Evicted vs node drain) is unconfirmed — needs cluster event access (kubectl/Grafana, see #8). Single transient blip, service recovered; confirm opportunistically once Grafana is wired.

**Next focus (set 2026-07-01, post-0900) — HIGH-activity hour, sx ENS/address work:**
- **A. snapshot.js#1203 push decision (await Wan).** ChaituVR owns the open sequencer-side ENS fix (green/mergeable). Only outstanding = Wan's fallback-catch narrowing (`CALL_EXCEPTION` + re-throw infra, same as sx#2143). Once Wan says go, push the narrowing to Chaitu's branch + relay to Chaitu (he owns it). Tracked in contacts/chaitu.md.
- **B. Sequencer `isSameAddress` swap (await Wan's go).** Add a local `isSameAddress` helper, replace the 16 ad-hoc `toLowerCase()` comparison sites across 6 writer files (delete-proposal, flag-proposal, settings, proposal, follow, moderation). Do NOT lowercase stored data (regresses dedup/leaderboard/follows — see skills/sx-address-normalization-checksum.md, the reason #2169 was closed).
- **C. Discord raid outcome.** User `1520075759685734462` mass-image-spammed 10+ channels; flagged to Less, recommended ban+bulk-delete, awaiting auth. Follow up on whether Less authorized + the moderation was executed. Loop stays read-only; this is a human-authorized action.
- **D. sx#2143** is approved (chai3-bot) — watch for merge.
- New skill: `skills/sx-address-normalization-checksum.md` (don't normalize at sequencer; ENS catch-narrowing pattern).
- **E. sx-monorepo PR #2197 (NEW 2026-07-01 ~12:00).** DRAFT opened (branch `fix/checkpoint-box-url` off master) replacing `https://checkpoint.box` → `https://checkpoint.snapshot.box` — docs/README only (4 files: README.md, docs/snapshot-x/services/api.mdx, apps/api/README.md, apps/delegates-api/README.md), no code, after-grep clean (negative-lookbehind to avoid double-replace), NO resync (confirmed to Less in #api). **Watch #2197 for review/merge** AND **await Less's confirm** on whether the `docs.checkpoint.box` → `docs.checkpoint.snapshot.box` subdomain change should also be added. See reports/2026-07-01-1200.md.

**Build follow-up (not a regression):** telegram-user **inbound** was never implemented (no MTProto/user train, bot tokens only). Track as a P2 build item.
