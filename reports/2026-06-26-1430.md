# Iteration report — 2026-06-26 14:30

Delta since reports/2026-06-26-0930.md. Only what's new this round.

## ChatKit PR #636 (bonustrack/stage) — deep-link support for Less
- Identified the PR concretely: "Kit JSON renderer + shared view builders (RN + Vue)", ~100 files.
- First gave the **web deploy-preview** link and verified it returns 200:
  `deploy-preview-636--stage-livenet.netlify.app/#/channels`.
- Less clarified he wanted the **native scheme**, not web. Provided the `stage://` deep links:
  - Conversation: `stage://xmtp/<convId>` → route `app/xmtp/[convId].tsx`.
  - Home / channels: `stage://` → default tab (no dedicated channels route exists).
  - Scheme defined in `apps/app/app.config.js:31`.
- **Caveat surfaced to Less:** `stage://` only resolves after installing the PR #636 Expo preview build (preview-launcher via the `u.expo.dev` group). It will not open in the stock app.
- CI status unchanged: overall gate **red** — build passes; lint / typecheck / knip / madge / test fail. Testable via preview, **not merge-ready**.

## Less's Stage transactions History screenshot
- Less shared a Stage **History** screenshot: **20,000 USDC received, Success**, timestamped Jun 26 1:26 AM.
- UI rendering cleanly: Queue / History / Messages tabs, Hide-suspicious toggle, Filter / Export controls.
- Not related to PR #636 or the SX vote work — flagged as a healthy-UI data point only.

## Memory repo
- Last run bootstrapped the repo. This run added the **contacts/** directory (per Less's request to track contacts); 8 contact files now present.

## Stations
- **Telegram-user station restored** — confirmed online. Less restored `TG_USER_*` creds after the cutover regression diagnosed last session. All **3 stations live**.

## Operational learnings (recorded to preferences/working-discipline as needed)
- Metro `send` / `react` MCP calls intermittently return a **300s idle-timeout ERROR even though the message actually sent**. Always verify by **reading the channel back** rather than resending — avoids duplicate posts.
- **Reacting to an inbound IMAGE/attachment can 404 ("Unknown Message")** if you use the upload filename id (e.g. `msg_<digits>_0.png`) instead of the real Discord message snowflake. The attachment note's filename is NOT the message_id — reply directly, or fetch the real id first.

## Carried-forward open items
- #2186 — mana self-register decision (self-register from `CommitAdded`/`_commits` vs UI persist+retry).
- #2183 — ready to merge pending nits.
- #2185 — merge after #2181 backend lands.
- Orphaned EthTx commits — no cheap re-trigger path.
- #636 — CI red, not merge-ready.
- Confirm Sekhmet → Wiktor / Amalio identity mapping (still pending).
