# Iteration report — 2026-06-26 18:30

Delta since reports/2026-06-26-1430.md. Only what's new this round.

## Pushed a Safe-signed Snapshot vote to the hub (for Less)
- Safe `0xCa1cc2DB71fBcA354ddc59507Eb7256112a3BFFb` (ethereum mainnet), via the Safe Transaction Service off-chain message flow.
- Vote details:
  - Space: `gnosis.eth`
  - Proposal: **GIP-151** (treasury pro-rata redemption), id `0x657fbf8892200d24e887c68245cee73b59c466394192be1c10673b39814c74c4`
  - Choice: `1` = **For**
  - Voting power: **28,185.41 vp**
  - Reason: "Vote FOR according to Futarchy.fi TWAP signal"
- The Safe message was **fully signed**: confirmationsSubmitted >= confirmationsRequired (2/3 threshold) with a non-null EIP-1271 `preparedSignature`.
- Submitted the EIP-712 envelope to https://seq.snapshot.org/ → returned **vote id `0x08b08bb2…28f54b`**, IPFS `bafkreibfdrrszl…zziu`.
- **Verified live** on the hub via hub.snapshot.org/graphql (vote present on the proposal).
- Sent Less the proposal URL: `snapshot.org/#/gnosis.eth/proposal/0x657fbf8892200d24e887c68245cee73b59c466394192be1c10673b39814c74c4`.

### Gotcha (load-bearing)
- The sequencer **rejects the envelope if `choice` / `timestamp` are JSON strings** — they must be sent as JSON **numbers**. The EIP-1271 signature stays valid either way (same uint hash is recovered), so this is purely a sequencer-side type check, not a re-sign.
- Guardrail applied: **only submit if the Safe message is fully signed** (confirmations met + preparedSignature non-null). Otherwise the hub would reject the EIP-1271 verification.

## Metro send idle-timeout false-error — persisted
- The known Metro `send` 300s idle-timeout ERROR-but-actually-sent behavior recurred again this round. Verified the message landed by reading the channel back. No new behavior; noting only that it persists. (Already captured in preferences/working-discipline.)

## New skill
- Added **skills/push-safe-signed-snapshot-vote.md** — playbook for fetching a Safe off-chain message, verifying it's fully signed, and POSTing the EIP-712 snapshot vote to the sequencer (numbers-not-strings rule + fully-signed guardrail + hub verification).

## Carried-forward open items (unchanged)
- #2186 — mana self-register decision.
- #2183 — ready to merge pending nits.
- #2185 — merge after #2181 backend lands.
- Orphaned EthTx commits — no cheap re-trigger path.
- #636 — CI red, not merge-ready.
- Sekhmet → Wiktor / Amalio identity mapping (still pending).
- (The Safe vote is fully done — nothing carried forward from it.)
