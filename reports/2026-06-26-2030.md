# Iteration report — 2026-06-26 20:30

Delta since reports/2026-06-26-1845.md (and the Amalio-mapping commit 4913c2d). Only what's new this round.

## Trusted-5 mapping COMPLETE
Amalio's Discord identity is confirmed: **`amaliohidalgo`** (global_name "Amalio Hidalgo"), id **1285995520174592085**. Learned via the 2h inbox-sweep catching Less @-mention him in the **gtm** channel. All 5 trusted Snapshot Labs members are now mapped:
- Less = `bonustrack_` (■)
- Wan = `wa0x6e` (▶)
- Chaitu = `chaituvr` (▶)
- Wiktor = Sekhmet / `0cf5e` (▶)
- Amalio = `amaliohidalgo` / 1285995520174592085 (▶)

This was itself a win for the **inbox-sweep loop**: the sweep surfaced an identity that would otherwise have stayed a stub. (Amalio mapping itself was already committed in 4913c2d; this report records the milestone + the gtm context below.)

## Discipline note — reply only when addressed
In the gtm channel I replied to Less's "Do you use typefully?" but it was directed at Amalio, not Metro. Less corrected: **"Not asked u"**. Lesson: when a message isn't @-addressed to Metro and lives in a channel where humans talk to each other, don't assume it's for me — **check who the message is aimed at before replying.** Captured in skills/metro-comms-reliability.md (comms-etiquette section).

## gtm / Typefully context (FYI, not a task)
Amalio uses **Typefully** as an intermediate step between JARVIS content approval and analytics:
- Analytics is a Typefully pro feature (he can use the X API for it).
- Typefully limits posts to **15/month**, so pushing drafts via API isn't working — he'll rewire to **post from the dashboard** instead.
Recorded as light project/context for the gtm/marketing area + the JARVIS reference.

## SX vote pipeline — no new developments since 1845
PR **#2189** still OPEN, awaiting review (detection layer: `.merge()` upsert + read-back verify). Real-fix ladder unchanged: (1) client/relayer retry on the rpcError, (2) relayer self-heal (recommended), (3) write-durability (afterCreate ping / txn-wrap). #2186 root cause unchanged (DB-confirmed app-side pre-commit lost write).

## Carried-forward open items (unchanged)
- #2186 / #2189 — as above. #2183 verified healthy. #2185 — merge after #2181 backend.
- #636 — CI red, not merge-ready.
- Metro daemon reliability — still an operational item; mitigated by the 2h inbox-sweep loop.
