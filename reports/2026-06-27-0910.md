# Iteration report — 2026-06-27 09:10

Delta since reports/2026-06-27-0812.md. (KB expansion skipped this tick — focused on comms-reliability + PR #2189 rework.)

## Comms reliability — dedicated 15-min poll for SX Ledger thread
- **Wan flagged Tony is missing too many messages on the SX Ledger thread** (`metro://discord/d0/1517956186929102869`) and asked to **"use a poll to monitor the thread."** → Added a **dedicated 15-min read-back poll cron (job `ac435a5e`)** for that thread, separate from the hourly loop. It read-backs the thread and replies to anything new/unhandled.
- Reinforces the established mitigation: **read-back is reliable, push is lossy.** Updated `LOOP.md` PART 1 (hot-threads poll note, list is tunable) and `skills/metro-comms-reliability.md` accordingly.

## #2189 — reworked to observability-only (Wan: "update the PR, don't change behavior, only add logging")
- Reverting the behavior changes: `.merge()` → back to `.onConflict().ignore()`; remove the rpcError-on-missing-row; keep always-rpcSuccess.
- Add ONLY a **warn log when an insert persists zero rows** so the lost write surfaces in Better Stack source `mana`.
- (Actual PR edit handled by a separate worker; recorded here for state.)

## Carried forward (no change)
- **#2125 split** — still AWAITING Less/Sekhmet go.
- **checkpoint#390** — relayed (sekhmet-review), not on GitHub.
- **Metro daemon** — still DEGRADED (inbound drops + outbound timeouts); restart likely.
