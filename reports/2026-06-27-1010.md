# 2026-06-27 10:10 — delta

Short delta vs 0910.

## #2189 — review thread with Wan (refined understanding; may not be worth merging)
Clarified in the review thread that master ALREADY has a **handler-level try/catch** — any THROWN error in the register path is already caught and 500'd. So the try/catch added in #2189 is **redundant**: it can't catch anything that wasn't already caught.

The real lost write is a **SILENT 0-row no-op**: `db.ts` `registerTransaction` uses `.onConflict(['sender','hash']).ignore()`, which on a conflict (or a non-durable write at the psdb gateway) **returns 0 rows and does NOT throw**. No try/catch — handler-level or added — can catch a no-throw. Only one of these surfaces it:
- **PlanetScale Query Insights** (dashboard) — see the actual per-statement behavior, OR
- **rowCount inspection** — check `.returning('id')`/affected-rows after the insert and act on 0, OR
- **dropping `.ignore()`** (switch to `.merge()`) so a real conflict/error can surface.

**Conclusion relayed to Wan:** #2189 as-written may not be worth merging (redundant try/catch + still-silent no-op). Asked Wan to pick: **(a)** park #2189, and on next occurrence use Query Insights to get the real per-statement error; or **(b)** a debug branch that deliberately lets errors surface (drop `.ignore()` / inspect rowCount). **AWAITING Wan's decision.**

## KB — mcp batch (PART 3)
Added `knowledge-base/mcp/snapshot-mysql.md`, `knowledge-base/mcp/oauth-gated-and-misc.md`, `knowledge-base/built-in-tools.md`; updated `mcp/README.md` + `PROGRESS.md` (per-MCP map effectively complete; next focus = structure/refactor pass).
- **Reachable:** Snapshot_MySQL (read-only `snapshot-hub@replica`, 12 tables; proposals/votes schema captured — names only, no rows); Slash (`list_endpoints`, 100+ endpoints — banking/spend API, financial PII so schema-only, only `GET /tokens/prices` is safe-public).
- **Stubbed / not exercised:** Cloudflare, Calendly, Docusign = OAuth-gated (only authenticate/complete_authentication exposed; auth NOT started — verify in main session). Browserbase tools listed (start/navigate/act/extract/observe/end) but stateful/agentic — no session opened.

## Carried (unchanged)
- #2125 split — AWAITING GO (stacked-branch plan ready, not pushed).
- checkpoint#390 (`@computed`) — utACK/COMMENT relayed to Less; not posted to GitHub; #2125 depends on it.
- Metro daemon — DEGRADED both directions (inbound drops + outbound 300s timeouts); restart likely needed. Read-back still reconciles inbound.

## Skill updated
`skills/diagnose-sx-vote-pipeline.md` — added the refined #2189 understanding (silent 0-row no-op vs throw; try/catch can't catch a no-throw; Query Insights / rowCount / drop-`.ignore()` are the only ways to surface it; #2189 may be parked).
