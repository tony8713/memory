# 2026-06-27 11:10 — delta

Short delta vs 1010.

## #2189 — CLOSED (Wan's call); branch kept
Wan decided to **close PR #2189**. Branch (`fix/mana-register-transaction-verify-write`) kept for reference. Rationale recorded:
- The existing **handler-level try/catch already covers throws** — an added try/catch is redundant.
- The lost write is a **silent 0-row / non-durable write with no throw** → no try/catch can catch it.
- The existing **"Registering transaction" log already carries sender + hash**, so diagnosis isn't blocked by missing logging.

SX vote saga is now: **diagnosis complete, awaiting PlanetScale Query Insights** (the only remaining source of truth for the per-statement behavior).

## Conclusive mana-log dig on the two missing votes — NOTHING anomalous
Dug the two missing-vote timestamps (15:11:13Z & 19:48:55Z) in Better Stack source `mana` (`mana_pino`). Findings:
- **register logged**, **pid 19 throughout** (no process restart), **no "Failed to register transaction"**, **no connection/pool/timeout** events, the processing loop was **uninterrupted** — yet **no row** persisted.
- Only **unrelated** errors in the window: 3095× "Failed to process proposal" (starknet RpcError / herodotus) starting 19:58; some "timestamp in the future"; "Failed to get data by message hash" (reads).
- **Conclusion:** silent 0-row / non-durable write at the **DB-gateway (PlanetScale psdb) layer** — invisible in app logs. **PlanetScale Query Insights for 15:11:13Z & 19:48:55Z is the only remaining source.** Asked Wan to pull it.
- Distinction reinforced: a `(sender, hash)` with **no row** can't be a duplicate-ignore (a duplicate-ignore implies the row already exists) — so this is genuine loss, not `.onConflict().ignore()` dropping a real duplicate.

## Carried (unchanged)
- **#2125 split** — still AWAITING GO (stacked-branch plan ready, not pushed).
- **checkpoint#390** (`@computed`) — utACK/COMMENT relayed; not posted to GitHub; #2125 depends on it.
- **Metro daemon** — DEGRADED (inbound drops + outbound 300s timeouts); restart likely. Read-back still reconciles inbound.

## KB — structure/refactor pass (PART 3)
- `INDEX.md` now links **everything** (mcp/ + per-server files, channels/, built-in-tools.md, contacts/README, every skill/preference/memory file, all 4 top-level KB files).
- `mcp-servers.md` demoted to a **short pointer/index** into `mcp/` (de-duplicated against the now-complete per-server files; no info lost).
- Cross-linked **contacts/ ↔ channels/** (channels README roster → contact files; each trusted-5 contact → key channels).
- `PROGRESS.md` updated: per-MCP map **COMPLETE (ongoing maintenance)**; structure pass **done**; next focus = ongoing maintenance + verify the 2 remaining Discord lines + optional Google Workspace de-stub.

## Skill / STATUS updated
- `skills/diagnose-sx-vote-pipeline.md` — #2189 marked **CLOSED**; added "logs exhausted → Query Insights is the source of truth" conclusion + the duplicate-ignore-vs-genuine-loss distinction.
- `STATUS.md` — #2189 **CLOSED**; SX vote saga = diagnosis complete, awaiting Query Insights; #2125 split awaiting go.
