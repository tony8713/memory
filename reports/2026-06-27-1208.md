# 2026-06-27 12:08 — delta

Short delta vs 1110.

## SX vote — Wan sent two logs, BOTH the wrong type
Wan provided two logs to chase the per-statement DB behavior. Neither contains query/INSERT data:
1. **`mana-main-logs.csv`** — PlanetScale **infra chatter** (Patroni / pgbouncer), a ~78-sec slice on 06-27. **No query/statement data at all.**
2. **Better Stack Postgres statement-log screenshot** — filtered to `registered_transactions` over 06-20→06-27, shows **ZERO INSERTs** (only a `pg_stat_statements` meta-query + one `select … where processed` read).

**Conclusion:** Postgres **statement logging is NOT capturing INSERTs** (`log_statement` is not `mod`/`all`). So the logs can't retro-diagnose the two past missing votes — those are **unknowable from logs**.

## Proposed forward fix (no app change)
To catch the *cause* on the next occurrence, proposed flipping a Postgres setting on the mana DB:
- `log_statement='mod'` (or `log_min_duration_statement=0`) so future INSERTs **and their errors** get logged, then **wait for the next occurrence**.
- This is server-config only — no app/PR change. (PlanetScale **Query Insights** remains the alternative source-of-truth path.)

**AWAITING Wan's decision** on flipping `log_statement`.

## Carried (unchanged)
- **#2189** — CLOSED (prior tick). Branch kept.
- **#2125 split** — still AWAITING GO (stacked-branch plan ready, not pushed).
- **checkpoint#390** (`@computed`) — utACK/COMMENT relayed; not on GitHub; #2125 depends on it.
- **Metro daemon** — DEGRADED this session (many send/read transport-drops); mitigated by read-back + the 15-min Ledger poll.

## Skill / STATUS updated
- `skills/diagnose-sx-vote-pipeline.md` — added that Postgres **statement logging wasn't capturing INSERTs** (`log_statement` not `mod`/`all`) so logs can't retro-diagnose; forward fix = `log_statement='mod'` / `log_min_duration_statement=0` or Query Insights.
- `STATUS.md` — pointer bumped to this report; SX saga now "awaiting `log_statement` flip / Query Insights".
