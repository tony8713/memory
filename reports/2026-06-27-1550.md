# 2026-06-27 15:50 — delta

Short delta vs 1508.

## SX Ledger 15-min poll cron (`ac435a5e`) — RETIRED
- The daemon recovered (push delivery + sends healthy, gap-free sweeps across all stations) and the SX Ledger thread has been **dormant ~11h** (vote saga parked, awaiting Wan's `log_statement` decision), so the dedicated 15-min read-back poll was steady-state overhead.
- The **hourly PART 1 inbox sweep already covers** `metro://discord/d0/1517956186929102869`.
- **Re-arm a short-interval poll only if drops recur OR a thread goes hot again.** (LOOP.md PART 1 + skills/metro-comms-reliability.md now frame hot-thread polls as arm-during-degradation / retire-on-recovery.)

## Carried (unchanged)
- **SX vote** — still AWAITING Wan's `log_statement='mod'` / Query Insights decision.
- **#2125 split** — AWAITING GO (stacked-branch plan ready, not pushed).
- **checkpoint#390** — utACK/COMMENT relayed (not on GitHub); #2125 depends on it.
- **stamp #468** — DROPPED (retracted by Wan); other agent retains it.
- **Metro daemon** — healthy.
