# 2026-06-29 17:11 — delta

First substantive report since `2026-06-27-2008` (ticks 28–29 were quiet/heartbeat). Captures the 2026-06-29/30 comms session.

## New this session
- **"x" MCP — DEFINED BUT UNREACHABLE (not running).** Configured in `~/.claude.json` as a static HTTP endpoint `http://127.0.0.1:8000/mcp` with **no launcher**; nothing listens on :8000. → The BOOTSTRAP item *"X MCP just installed → like queued @SnapshotLabs tweets + add an X auto-like loop step"* is **BLOCKED** (X MCP unreachable). Do **NOT** attempt the X auto-like step. X remains reachable only via **Zapier X actions** or the X API directly (see `knowledge-base/socials-and-external.md`). Mapped in `knowledge-base/mcp/oauth-gated-and-misc.md`.
- **boorger.eth expired — NO ecosystem impact (personal to Boorger).** Flagged in Discord #workflow. Resolves `0x1210…a3DC` (personal Ledger EOA); expired 2026-03-17, grace ended 2026-06-15, now past grace / in premium-decay auction (~4.2 ETH start, ~2 weeks to normalize). Verdict: **not** a Snapshot space admin/member/mod, no proposals, zero refs in Stage/bonustrack code or config → personal to Boorger only. No team action.
- **sx-monorepo #2194 — Sekhmet review POSTED (utACK).** `DECIMAL(13,3)` schema aligns with the sequencer; `formatAddresses` consistent with the votes resolver. No inline comments. (Real GitHub post, via sekhmet-review skill.)
- **Discord etiquette convention (team feedback, #workflow 2026-06-29):** in Discord channels, do **NOT** treat every message as directed at the bot — respond only when **@-mentioned, replied-to, or clearly actionable**. Reflected in `preferences/working-discipline.md` (item 6); a local Claude-Code memory was also saved.
- **sx-monorepo #2185 rebase — BLOCKED, awaiting Less.** Missed Discord request (Metro dropped event) now picked up. The branch's backend commits are **superseded by #2181 on master**; plan is to **cherry-pick only the UI commit `66db2dfd`** once Less confirms. Reported to Less; awaiting his decision.
- **checkpoint #390 — addressing review comments** (separate worker in progress). Second previously-missed Discord request, now picked up.

## Daemon degradation (NEW — partial)
- **XMTP sending BLOCKED by a persistent per-IP rate limit** on `GetIdentityUpdatesV2` ("resource exhausted", node `R152.233.42.56DEF`). Has persisted **~1+ day across a daemon reconnect**. **Reads still work** (`QueryGroupMessages` OK) — so this is send-path-only on XMTP, distinct from the 2026-06-27 both-directions degradation that recovered. A separate worker is investigating. Discord/Telegram unaffected.
- Hot-thread poll: not re-armed. Per LOOP.md PART 1, the degradation is **XMTP-send-only** (reads fine, Discord fine) and no XMTP thread is currently hot, so the hourly PART 1 sweep still covers reads. Re-arm a short-interval poll only if an XMTP thread goes hot while sends remain blocked.

## Unchanged / still awaiting humans (carried from STATUS)
- **SX vote saga** — AWAITING Wan's `log_statement='mod'` / Query Insights decision.
- **#2125 split** — AWAITING GO (stacked-branch plan ready, not pushed; depends on #390).
- **stamps #468 / #469 / #476** — all with the other agent (`1471598965756788970`); not Tony's.
