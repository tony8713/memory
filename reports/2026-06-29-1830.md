# 2026-06-29 18:30 — delta

Delta vs `reports/2026-06-29-1711.md`. Same comms session, continued.

## PR work — #2125 split EXECUTED
- **#2125 split is DONE (not just planned).** #2125 is now **backend-only**: force-pushed `9f51f96e`, reverted the 4 UI files back to merge-base so only the 2 API files remain. The UI half moved to a **new stacked PR #2195** (branch `feat/active-proposals-explore-ui`, base = `feat/active-proposals-explore`). **Retarget #2195 → master after #2125 merges.** Sekhmet gave the explicit "execute the split" go. (Supersedes the prior "AWAITING GO / not pushed" state.)
- **`active_proposal_count` `@computed` field — VERIFIED working.** Queried live; resolver SQL is sound and the where-filters generate correctly. Two branch fixes flagged to Sekhmet:
  1. **Bump `apps/api` checkpoint dep to `>= 0.1.0-beta.73`** — branch still pins `beta.70`. (For the live test I bumped to beta.73 locally; that's how it was verified — answered Sekhmet's follow-up on method.)
  2. **Rebase the branch on master** — it's missing `quorum_finalized` from #2172, so proposal inserts fail / index 0 rows until rebased.
- **checkpoint #390 — confirmed already MERGED + released `v0.1.0-beta.73`.** Sekhmet's review comments were all in master pre-merge (the earlier "addressing review comments" worker found nothing left to fix). Reported to Less. (Supersedes prior "addressing review comments in progress".)

## XMTP send outage — diagnosis COMPLETE, fix deploying (supersedes prior "investigating")
Escalation chain, each step ruled out the next:
1. Removed the **duplicate local XMTP client** (two clients on one inbox).
2. **Revoked stale installations** → inbox now **1/10** (was near the 10-installation cap).
3. Still rate-limited after revocation.
4. **Root cause:** the cloud `tony` XMTP client **never boots** because the **raw-privateKey loader patch is MISSING from the deployed Fly image**. The patch exists only as a **local uncommitted edit** in `~/work/metro-protocol/packages/xmtp/src/accounts.ts`; the deployed code has **no `XMTP_TONY_PRIVATE_KEY` reference**. The per-IP rate bucket `R152…DEF` is keyed on the **Fly machine's shared egress IP**, already exhausted, and a restart does **not** clear it.
- **Tony approved fix: "Deploy + `DERIVE_COUNT=1`."** A worker is deploying the loader patch + setting `DERIVE_COUNT=1` now. **In progress.**
- **STATUS:** XMTP **send** degradation ongoing; **reads OK**; cloud deploy in progress. This supersedes the prior report's note that the XMTP probe "should continue" — it's now an active deploy, not a passive probe.

## Other
- **Chaitu ↔ Notion confirmed connected.** Chaitu then asked for **Avalanche info in Notion for premium networks** — a worker is handling that lookup now (**in-progress**).
- **#2185 rebase — still BLOCKED**, awaiting Less's decision (cherry-pick UI commit `66db2dfd` only). Unchanged.
- **"x" MCP — still unreachable** (no launcher); X auto-like step remains BLOCKED. Unchanged.

## Carried unchanged
- SX vote saga — AWAITING Wan's `log_statement='mod'` / Query Insights decision.
- stamps #468/#469/#476 — with the other agent (`1471598965756788970`); not Tony's.
