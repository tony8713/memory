# 2026-06-30 09:00 — delta

Delta vs `reports/2026-06-29-1830.md` + the quiet-tick STATUS updates since (commits `9753b2d` read-verb degradation note, `41dd43e` write-timeout learning, `2f57d64` hardening plan).

## 1. MCP stream-wedge — ROOT-CAUSED + FIXED + DEPLOYED (the big one)
Supersedes every prior "read-verb degradation / 300s `read` timeouts / post-deploy MCP-stream flakiness / zombie stream → `/mcp` reconnect" note. That whole class of symptom is now **FIXED at the source**, not worked around.

- **Root cause** (`apps/mcp/src/mcp/index.ts`): a **single global** `StreamableHTTPServerTransport` that **`rebind()`'d** (close + new session id) on *every* session-id mismatch. Consequences:
  - Tore down in-flight reads → the repeated **300s hangs**.
  - Orphaned the inbound SSE stream.
  - Any **2nd concurrent client** caused **permanent mutual-rebind thrash** (each client's request kept stealing/closing the other's transport).
  - The **18:33Z deploy bounce** kicked off the reconnect storm that wedged the orchestrator's stream.
- **Fix (commit `04f6b6f`):** **per-session transports** — `Map<sessionId, {server, transport}>`; create a transport **only on `initialize`**; route subsequent requests by `mcp-session-id`; **404** on unknown/stale ids (**never close or steal a live transport**). Removed the `rebind` / `syncSession` / `_webStandardTransport` hack. Inbound relay + 25s keepalive now **broadcast per-session**.
- **Verified:** build clean, **137 tests pass**, deployed to Fly (app `metro`), checked live — per-session sessions work; a bogus session id returns **404 without disturbing the live session**; no rebind churn. **Reads now return instantly** from the orchestrator.
- **Deploy-drift also committed (`2f9a5fa`):** `packages/xmtp` `accounts.ts` raw-privateKey loader patch + previously-untracked `packages/mcp`, `packages/metro`.
- **OPEN FOLLOW-UP (carry):** both fix commits (`04f6b6f`, `2f9a5fa`) are on **LOCAL main only — NOT pushed to origin**. The branch is **5 behind origin** due to unrelated telegram-user commits. Cloud already runs the fix (Fly deploys from the working tree), but **origin needs reconciling + push**.

## 2. Local `box.metro.dispatcher` daemon — RETIRED
`launchctl bootout` (plist preserved for revival). Eliminates the competing 2nd MCP client that drove the multi-client rebind thrash *and* the earlier XMTP installation-cap rate-limit. **One client per inbox now.** (Closes the "optional cleanup" flagged in the prior PROGRESS next-focus.)

## 3. Hardening plan written
`knowledge-base/hardening-plan.md` (commit `2f57d645`) — 7 failure modes, P0/P1/P2.
- **P0 DONE:** MCP transport fix (#1 above), retire local dispatcher (#2).
- **P1/P2 remaining:** deploy-drift preflight; account-config pinning; inbound liveness monitoring/alerting; reconnect backfill/resumability (skipped — needs an `EventStore`); telegram-user MTProto train (never implemented).

## 4. Inbound diagnosis
XMTP cloud train **healthy** (`x0` receives Less; `DERIVE_COUNT=1` harmless — Less uses `x0`). **telegram-user inbound was NEVER implemented** — no MTProto/user train exists, only bot tokens. That's a **build follow-up**, not a regression.

## 5. #2125 — live-data re-verification IN PROGRESS
`active_proposal_count` was earlier verified via **synthetic** rows; **Sekhmet pushed back** (an empty source set proves nothing). A proper **live-data re-verification** (real non-empty source, real active proposals) is now **in progress — awaiting result.**

## 6. Comms caught up post-fix
- Answered Sekhmet's synthetic-row question + her pushback (**conceded** the point).
- Acked Less's interns banter + avatar-change ping.
- **#2185** still awaiting Less's rebase decision (cherry-pick UI commit `66db2dfd` only).

## Carried unchanged
- #2185 BLOCKED on Less; "x"/`unreal-mcp` MCP unreachable (dead `127.0.0.1:8000`); SX vote saga AWAITING Wan's `log_statement='mod'` decision; checkpoint #390 merged + released `v0.1.0-beta.73`.
