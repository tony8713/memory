# Tick report — 2026-06-30 ~15:00 UTC (delta)

Short delta tick. No new Metro asks worked this tick; the substance is a KB correction + two task-state flips.

## 1. X MCP status CORRECTED (supersedes the stale ":8000 down" note)
The old `x` MCP entry (dead `http://127.0.0.1:8000/mcp`, no launcher) has been **REPLACED** by a connected cloud entry **`x-cloud` → `https://api.x.com/mcp`** — X's official hosted MCP ("xmcp"), **bearer-auth** (token NOT recorded; secret). `claude mcp list` shows it **✔ Connected** and reachable: `initialize` + `tools/list` succeed, exposing **24 tools**.

**But it is READ / BOOKMARK-ONLY.** Tools: `search_posts` / `search_news` / `search_users`, get timelines / mentions / trends, `get_users_me`, and create/delete **bookmarks**. There is **NO `create_post`/tweet tool**.

Net: **X-posting is STILL unavailable** — now for a different reason than before. Not "MCP down" but **token access tier = read + bookmark, no publish**. Also the `x-cloud` read tools aren't loaded in the current loop session (entry added after session start) → needs a **reconnect / `/mcp` refresh** to surface them.

To enable posting (awaiting Less's choice): (a) a **write-capable X token tier** (exposes create-post on `x-cloud`); (b) **Zapier X write** via OAuth; (c) **Typefully** (15-post/mo cap).

KB updated: `knowledge-base/socials-and-external.md`, `knowledge-base/mcp/README.md` (reconciliation table + takeaway), `knowledge-base/mcp/oauth-gated-and-misc.md` (section retitled `x-cloud`).

## 2. Sekhmet #interns open-PR origin-trace — DONE
Bot delivered references for all 9 open sx-monorepo PRs under her #interns anchor: Discord origins #2195/#2185/#2086/#2122 (jump links); off-Discord-but-documented #2103 (Fabien via Telegram, recorded in #api), #2168 (BetterStack logs), #2143 (Intercom); #2169/#2119 have no reachable Discord origin (likely Telegram — not searchable until telegram-user inbound is built). **7/9 referenced; the 2 gaps are the telegram-user tooling limit, not a miss.** Marked DONE in STATUS + PROGRESS.

## 3. Open / awaiting-human (Last checked ~2026-06-30 15:00 UTC)
- **Chaitu** — schnaps-api #21 design (competing-architecture consolidation; team settling in #api).
- **Less** — X-posting enablement path (just clarified x-cloud is read-only); Stripe live-key wiring (gated on #21); #2185 👍 on cherry-pick-UI-only.
- **#general community support** — held question (knoshua, premium voting strategies); bot awaiting Less's nod on whether to field #general at all.
- **Origin-push of MCP fix commits** `04f6b6f` + `2f9a5fa` (PROGRESS next-focus #1, still pending) — cloud runs the fix; origin needs reconcile + push.
