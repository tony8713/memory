# Tick 2026-06-30 1608 UTC — DELTA

Baseline: reports/2026-06-30-1500.md. Inbox for this hour (#pineapple-debug + tony channels) was handled by the main Metro session; this tick folds those outcomes into memory rather than re-investigating.

## New substance this hour

### 1. Better Stack MCP — RECONNECTED + working
The `claude_ai_Better_Stack` connector is back to ✔ Connected and read calls work again. Prior intermittence cleared. No action; monitoring/logs queries are available to the loop again.

### 2. X / Twitter MCP — confirmed app-level only (still cannot post)
Re-confirms the 1500 correction: `x-cloud` (`https://api.x.com/mcp`) is **app-context / read+bookmark only**. Posting needs **user-context OAuth** (a write-capable token tier bound to a user), which is not provisioned. X-posting remains UNAVAILABLE. Awaiting Less's chosen enablement path (write token / Zapier X OAuth / Typefully). No new capability gained.

### 3. Pineapple outage RCA — COMPLETE (infra-level pod kill)
Incident: **2026-06-28 22:31 UTC** pineapple pod restart, **~11 min of log silence**.
- Pod: `pineapple-54d65f6684-6ljs4`.
- **No deploy** correlated, **no master merge**, **no Sentry error** in-window → rules out an app-side crash or a shipped regression.
- Conclusion: **infra-level pod termination** — k8s pod kill / OOM / node eviction.
- **Unconfirmed:** the k8s events for `pineapple-54d65f6684-6ljs4` (would distinguish OOMKilled vs Evicted vs node drain) are still not retrieved — needs cluster `kubectl get events`/describe access, which the loop session doesn't have. Open follow-up, low urgency (single transient blip, service recovered).

### 4. Grafana MCP — re-add requested
**wa0x6e asked Less to re-add Grafana to tony's MCP.** Not yet present in the connector set (`claude mcp list` has no Grafana). Pending Less wiring it. Once added it gives the loop a second observability source (k8s/infra dashboards) that would let pineapple-style RCAs confirm pod-kill cause directly instead of leaving k8s events unconfirmed. Tracked as a capability-pending item.

## Carried-forward unchanged
All STATUS open items from 1500 stand (X-posting enablement awaiting Less; schnaps-api #21 consolidation awaiting human decision in #api; Stripe wiring gated on #21; #2125 awaiting review/merge then retarget #2195; #2185 cherry-pick awaiting Less; #general scope awaiting Less's nod; MCP fix commits 04f6b6f+2f9a5fa still local-main-only, origin push pending).

## KB touched
- `knowledge-base/repos-and-infra.md` / `mcp/better-stack.md` context: pineapple = Better Stack-monitored service; RCA recorded here.
