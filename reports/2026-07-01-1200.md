# Report — 2026-07-01 ~12:00 UTC

DELTA vs reports/2026-07-01-0900.md. Inbox handled by main Metro session; one new substantive item below, carryovers unchanged.

## NEW — sx-monorepo PR #2197 (checkpoint.box → checkpoint.snapshot.box) — DRAFT opened

- Less (■) asked in #api for a PR replacing `https://checkpoint.box` with `https://checkpoint.snapshot.box` across sx-monorepo.
- **Opened DRAFT PR snapshot-labs/sx-monorepo#2197**, branch `fix/checkpoint-box-url` off `master`.
- **Docs/README only — 4 files, NO code:** `README.md`, `docs/snapshot-x/services/api.mdx`, `apps/api/README.md`, `apps/delegates-api/README.md`.
- Used a **negative lookbehind** in the replace to avoid double-replacement (no `checkpoint.snapshot.snapshot.box`); **after-grep clean**.
- Confirmed to Less in #api that it triggers **NO resync** (docs-only, no runtime/config change).
- **Open question (await Less):** whether the `docs.checkpoint.box` → `docs.checkpoint.snapshot.box` **subdomain** change is also wanted (not included in #2197 yet).

## Carryovers (unchanged this hour)
- **A.** snapshot.js#1203 catch-narrowing push — await Wan (▶).
- **B.** Sequencer `isSameAddress` swap of 16 sites across 6 writer files — await Wan (▶).
- **C.** Discord raid user `1520075759685734462` — await Less's ban authorization; no destructive action taken (loop is read-only).
- **D.** sx#2143 approved (chai3-bot) — watch for merge.
